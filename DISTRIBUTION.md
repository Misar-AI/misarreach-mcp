# Distribution

This package is published to npm as `@misarreach/mcp`.

## Publishing

```bash
pnpm build
npm publish --access public
```

## MCP Registry

Listed on smithery.ai and mcp.so. Update `smithery.yaml` and `server.json` on version bumps.
